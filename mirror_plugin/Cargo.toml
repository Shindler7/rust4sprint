[package]
name = "mirror_plugin"
version = "0.1.0"
edition = "2024"
description = "A plugin for iproc that mirrors an image vertically and/or horizontally."

[dependencies]

[build-dependencies]
anyhow.workspace = true
cc.workspace = true

[lib]
crate-type = ["cdylib"]
